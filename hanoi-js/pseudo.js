class Hanoi {
  run() {
    // until a pyramid of disks is on another column
      // get column from the player
      // validate move
      // move top disk to assigned column
      // evaluate win state
  }
}
